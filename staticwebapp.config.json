{
    "routes": [
        {
            "route": "/admin",
            "allowedRoles": ["authenticated"]
        },
        {
            "route": "/dashboard",
            "redirect": "/admin"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}